<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude specific files or directories from cloud backup if needed -->
    </cloud-backup>
    <device-transfer>
        <!-- Exclude specific files or directories from device transfer if needed -->
    </device-transfer>
</data-extraction-rules>
